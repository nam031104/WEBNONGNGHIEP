spring:
  application:
    name: iot
  datasource:

    url: jdbc:sqlserver://localhost:1433;databaseName=iot;encrypt=true;trustServerCertificate=true;sendStringParametersAsUnicode=true;
    username: sa
    password: "123456"
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:

      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect

mqtt:
  uri: tcp://broker.emqx.io:1883